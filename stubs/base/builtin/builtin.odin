package builtin

// Placeholder for `base:builtin`.
//
// In upstream Odin, `base:builtin` hosts fundamental value types and helpers
// that the rest of the collection layer is built upon (`any`, `Context`,
// allocators, etc.). Most of those already exist as compiler built-in types in
// this implementation (`any`, `rawptr`, `Context`, `Allocator`, ...), so this
// package is intentionally empty for now.
//
// Add real declarations here as cross-package stubs grow to depend on them.
